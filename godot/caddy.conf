:80 {
    root * /srv
    
    # Serve bundle schema
    handle /bundle.schema.json {
        respond /bundle.schema.json
    }
    
    # Health check
    handle /health {
        respond "OK" 200
    }
    
    # Log requests
    log {
        output stdout
    }
}
